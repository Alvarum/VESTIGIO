# F00 — Baseline y reconciliación

Fecha: 2026-09-20. Base examinada: `28dafa949ff68ed3dc52bf93d287863037bf8578` en `main`. Antes de crear este paquete, el checkout estaba limpio. El código de `include/`, `src/`, `tests/`, `tools/`, `CMakeLists.txt` y `CMakePresets.json` no cambió entre `ae48d31` y esta base.

## Entorno observado

- Windows NT `10.0.26200.0`, proceso x64; CPU reportada por entorno como AMD64 Family 23 Model 113.
- GCC 16.2.0, Clang 22.1.8, CMake 4.4.3, Ninja 1.13.2 y .NET SDK 10.0.401.
- raylib fijada en `dbc56a87da87d973a9c5baa4e7438a9d20121d28`; configuración CMake: `GRAPHICS_API_OPENGL_33`.
- WMI/CIM denegó acceso a `Win32_VideoController`; no se obtuvo fabricante/driver GPU. `retro_window` sí creó el contexto gráfico y pasó. G01 debe registrar `GL_VENDOR`, `GL_RENDERER` y `GL_VERSION` desde el contexto real, sin inventar el dispositivo.

## Matriz ejecutada

Primer intento: `./tools/check.ps1 -Full`, exit 1 antes de ejecutar el proyecto porque la política del host bloqueó scripts PowerShell. No se cambió esa política.

Comando efectivo:

```powershell
& 'C:\WINDOWS\System32\WindowsPowerShell\v1.0\powershell.exe' -NoProfile -ExecutionPolicy Bypass -File '.\tools\check.ps1' -Full
```

Resultado: exit 0.

| Preset | Pruebas | Resultado |
|---|---:|---|
| analyze | `retro_contracts` 1/1 | PASS, 0,31 s |
| ubsan | `retro_contracts` 1/1 | PASS, 0,21 s |
| debug | shared_session, studio_authoring, window, contracts 4/4 | PASS, 6,39 s; builds .NET con 0 warnings/errores |
| release | shared_session, studio_authoring, window, contracts 4/4 | PASS, 5,24 s; builds .NET con 0 warnings/errores |

Se ejecutó además `build/debug/retro_platform_test.exe`: exit 0, `PASS window resize, GPU readback, nearest scaling, letterbox, PNG load/export`. Este test verifica presentación y captura de un framebuffer CPU; no acredita rasterizado de geometría GPU y no cierra G01.

## Estado A01–A14 en el HEAD reconciliado

| Hallazgo | Estado actual y evidencia |
|---|---|
| A01 acoplamiento render→interaction | Pendiente: `src/engine/render.c` incluye `retro/interaction.h` |
| A02 sesión monolítica/juego incorporado | Pendiente: `src/session/session.c` aún concentra world, arte, UI, shooting, persistencia y render |
| A03 editor no es SDK mínimo | Pendiente: `include/retro/editor.h` expone contrato de tooling; CMake mantiene `WINDOWS_EXPORT_ALL_SYMBOLS` |
| A04 480×270 repetido | Pendiente: sesión, Player, FPS, lab, HUD y `GameViewport.cs` contienen dimensiones fijas |
| A05 texturas por definición sin registro compartido | Pendiente en lo esencial: se cargan imágenes por slot/material, pero no existe AssetId/caché compartida genérica |
| A06 fallos de textura provisional | Parcialmente corregido: `colors` propaga error; `create_pickup_sprites` aún no devuelve/verifica resultado |
| A07 marker sin TRS/padre/pivot | Pendiente: `ReMarker` conserva posición/yaw; no hay Transform general |
| A08 audio limitado/desconectado | Pendiente: plataforma conserva capacidad acotada y sesión no constituye sistema de assets/voces/música |
| A09 input/settings incrustados | Pendiente: 480/FOV/bindings permanecen distribuidos; Player sí pasa held, pero `GameViewport` requiere paridad completa en I01 |
| A10 historial copia `ReProject` | Pendiente: `RE_EDITOR_HISTORY=48` y snapshots completos siguen en `editor.c` |
| A11 snapshot incompleto de sesión | Pendiente: `ReSessionProjectile` vive en sesión y no forma parte del contrato general de save |
| A12 import no produce modelo/material genérico | Parcial: `material_files` carga imágenes reales; `re_project_import` continúa siendo copia de bytes y no hay modelo/shader/material asset general |
| A13 consultas/navegación especializadas | Pendiente: sectores/traces existentes no equivalen a mallas 3D, colliders rotados ni sweeps generales |
| A14 export copia raíz completa | Pendiente: `tools/export-project.ps1` usa `Get-ChildItem $projectRoot | Copy-Item -Recurse` |

No se reaplicaron las mejoras ya presentes en `ae48d31`. Los tests legacy, el documento transaccional, sectores/portales, sesión compartida y WPF se consideran contratos de preservación. F01 debe crear fronteras mínimas junto a ellos, sin renombrado global.

## Resultado

F00 verificado. Baseline reproducible y verde; deuda reconciliada; toolchain disponible. No se encontró un bloqueo para F01. El fabricante/driver GPU queda como dato pendiente que G01 puede obtener desde el contexto OpenGL.
