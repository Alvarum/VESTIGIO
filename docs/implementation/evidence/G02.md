# G02 — Superficie GPU embebida en Studio

Fecha: 2026-09-20. Base GPU integrada: `a13fb8beb89691d842709773abadb0b755d8f9a6`.

## Decisión de superficie

Studio usa un `HwndHost` WPF que aloja una ventana OpenGL 3.3 creada por raylib. El adaptador nativo obtiene el HWND creado por raylib, lo convierte en hijo del HWND provisto por WPF, elimina estilos de ventana superior y aplica `WS_CHILD`, clipping y tamaño físico. No intenta hacer que raylib adopte un HWND creado por WPF.

El hilo UI posee ventana, contexto y renderer. Sólo puede existir un `VgGpuHost` por proceso. `BuildWindowCore` crea el host y se suscribe al pulso de composición; `DestroyWindowCore` y `Dispose` quitan la suscripción, destruyen recursos GPU y cierran la ventana/contexto. El viewport actual dibuja la escena diagnóstica G01; E01/G03 sustituyen esa entrada por `EditWorld` y modelos reales. El `GameViewport` CPU legacy permanece debajo durante el spike para no romper Play.

El frame normal ejecuta mesh, shader, textura, depth y presentación en GPU. No copia el framebuffer a `WriteableBitmap`. `vg_gpu_host_capture` es una operación de prueba explícita que incrementa readbacks y no participa en `CompositionTarget.Rendering`.

## Pruebas ejecutadas

`retro_studio_authoring` cubre:

- 50 ciclos secuenciales create → render → resize 320×180 a 640×360 → render → destroy sobre un HWND WPF;
- integración real de `GpuViewportHost`, dos frames, resize a 426×240 y liberación explícita;
- documento AvalonDock `Probar` conectado una vez, composición y resize a 1280×800 y 1920×1080;
- captura solicitada desde el renderer alojado, decodificada como PNG 320×180.

Salida directa relevante:

```text
PASS GPU native host create/render/resize/destroy x50
PASS WPF HwndHost GPU render and resize
PASS WPF composition 1280x800, 1920x1080 (offscreen)
```

La prueba detectó durante desarrollo un segundo host activo al desacoplar sin `Dispose`; se corrigió haciendo explícita la liberación y manteniendo una superficie durante los cambios de docking/tamaño.

Captura reproducible local: `build/debug/studio-evidence/gpu-hwndhost.png`, SHA-256 `FAD19F4236FB870C9D0EE33BF283C63BF03A3787BD2EA18BD6A7264B53719E0B`. Inspección visual: fondo oscuro, cubo rojo con depth y billboard azul con alpha. El hash coincide con la captura G01 porque ambos recorren el mismo render target y escena determinista; G02 la obtiene a través del host WPF.

## Matriz final

Comando:

```powershell
& 'C:\WINDOWS\System32\WindowsPowerShell\v1.0\powershell.exe' -NoProfile -ExecutionPolicy Bypass -File '.\tools\check.ps1' -Full
```

Exit 0:

- analyze 4/4 y UBSan 4/4;
- debug 8/8; `retro_studio_authoring` 14,95 s, `vestigio_gpu_render` 0,75 s;
- release 8/8; `retro_studio_authoring` 14,36 s, `vestigio_gpu_render` 0,76 s;
- builds Studio y pruebas .NET: 0 advertencias, 0 errores.

Hardware/contexto heredado y vuelto a ejercitar por G01: NVIDIA GeForce RTX 5060 Ti, driver OpenGL `3.3.0 NVIDIA 616.56`, rlgl 3.

## Cobertura y límites observados

- PASS: superficie GPU nativa realmente embebida; no es una ventana externa ni un blit CPU por frame.
- PASS: ownership único, error ante segundo host, resize físico, docking conectado, cleanup y recreación 50 veces.
- PASS: 320×180 interno y presentaciones 426×240, 640×360, 1280×800 y 1920×1080.
- PARCIAL: el cálculo DPI usa `VisualTreeHelper.GetDpi` y convierte unidades WPF a píxeles, pero el entorno automatizado sólo expuso 96 DPI; 150 % y 200 % quedan para aceptación humana con monitor físico.
- NO VERIFICADO manualmente: foco real, Tab/Escape, minimizar/restaurar y arrastre entre monitores. Se intentó abrir el ejecutable y el log confirmó “Ventana visible”, pero la herramienta de computer-use disponible en esta sesión no expone aplicaciones nativas. No se convierte ese intento en PASS.
- PENDIENTE: input común I01, `EditWorld` E01 y escena/modelos World G03/G04.

G02 queda verificado dentro de las capacidades disponibles; las comprobaciones humanas pendientes están identificadas y no bloquean R01/R02 ni el backend GPU.
