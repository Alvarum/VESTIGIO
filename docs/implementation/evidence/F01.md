# F01 — Contratos mínimos v0.1

Fecha: 2026-09-20. Base integrada: `fe43b83de2f8579ecad715991de5c04252d61dd0`.

## Resultado implementado

- `include/vestigio/vestigio.h`: frontera pública C11/C++11, versión 0.1, macro de export controlada, resultados, backend/log enums de ancho público fijo, contexto opaco, handles de 64 bits, vectores/quaternion/TRS y descriptores con `struct_size/api_version`.
- `Vestigio::Headers`: target CMake de consumo que expone únicamente `include/`; no propaga las flags C23 internas.
- `src/render/render_contract.h`: contrato privado inicial de surface/draw packets/stats/ownership, sin incluir raylib, OpenGL, HWND ni WPF.
- `CONTRACTS.md`: C01–C08 pasan de propuesta a versión v0.1 y registran archivos, owners y consumidores.
- Pruebas C11, C++11 y contrato interno incorporadas a CTest. Se habilitó CXX sólo para verificar el consumidor C++.

No se creó un runtime ficticio. `vg_get_version` queda declarado y su definición pertenece al primer target funcional de R01/R03. World/entity/assets y surface son vocabulario/ownership congelado; todavía no hay lifecycle ni render GPU.

## Verificación

Primer intento del ticket: `tools/check.ps1 -Full` terminó antes de compilar por violaciones de `clang-format` en el header nuevo. Se formatearon exclusivamente los archivos nuevos con el `clang-format` del proyecto y se repitió toda la matriz.

Comando efectivo:

```powershell
& 'C:\WINDOWS\System32\WindowsPowerShell\v1.0\powershell.exe' -NoProfile -ExecutionPolicy Bypass -File '.\tools\check.ps1' -Full
```

Resultado final: exit 0.

| Preset | Resultado |
|---|---|
| analyze | 4/4: header C11, header C++11, render contract, retro contracts |
| ubsan | 4/4: mismos casos |
| debug | 7/7: tres casos nuevos + shared session, Studio authoring, window y retro contracts; .NET 0 warnings/errores |
| release | 7/7: mismos casos; .NET 0 warnings/errores |

Los tests de header comprueban layout estándar/POD-like, ancho de handles y versión. El test privado comprueba layout básico y declara explícitamente que el present normal no requiere readback. Es una prueba del contrato, no de que G01 ya lo implemente.

## Aceptación

- PASS: header mínimo consumible por C11 y C++11, sin tipos internos ni dependencias gráficas/plataforma.
- PASS: ownership, identidad, ejes/unidades, superficie y documento tienen reglas únicas en `CONTRACTS.md`.
- PASS: G01 y R01 reciben puntos de integración disjuntos (`src/render/gpu_raylib` frente a `src/runtime`/`src/world`); cambios futuros de `include/vestigio` o CMake se serializan.
- PENDIENTE por diseño: implementación/link del runtime, SDK instalable, mallas GPU y embedding WPF corresponden a tickets dependientes.

F01 verificado y listo para integración.
