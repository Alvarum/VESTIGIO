<UserControl x:Class="RetroForge.Studio.SceneItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinHeight="42">
  <Grid Margin="3,3,3,3">
    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
    <StackPanel>
      <TextBlock FontWeight="Medium" Text="{Binding DisplayName}" />
      <TextBlock FontSize="11" Foreground="{StaticResource MutedBrush}" Text="{Binding Subtitle}" />
    </StackPanel>
    <TextBlock Grid.Column="1" Text="›" Foreground="{StaticResource FaintBrush}" Margin="8,0" />
  </Grid>
</UserControl>
